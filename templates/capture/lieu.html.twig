{% extends 'base.html.twig' %}
{% block title %}Fiche Pokemon{% endblock %}

{% block body %}
{% include 'header.html.twig' %}
<style>
.content {
  display: flex;
  justify-content: space-between;
  padding: 10px 0;
}
</style>
<div class="card" style="width: 80%;margin: auto;margin-top:5%;">
<div class="card-body">

{{ form_start(CaptureForm) }}
{% form_theme CaptureForm 'form_errors.html.twig' %}
{{ form_row(CaptureForm.lieu) }}
{% if isTraining and isCapture %}
 <button type="submit" class="btn btn-primary mb-2">Capture</button>
 {% else %}
 <span>Indisponible</span>
 {% endif %}
        {{ form_end(CaptureForm) }}
<a href="{{ path('app_AccueilCapture') }}">retour à la liste</a>
</div>
    </div>
{% endblock %}
