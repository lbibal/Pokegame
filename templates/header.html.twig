{% extends 'base.html.twig' %}

{% block title %}Mon Site - {% endblock %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light container-fluid">
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                {% if not app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_default') }}">Présentation</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
                    </li>
                {% endif %}
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_accueil') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_listPokemon') }}">Liste des Pokemons</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_entrainement') }}">Entrainement</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href=" {{ path('app_AccueilCommerce') }}">Commerce</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Capture</a>
                    </li>
                {% endif %}
            </ul>
        </div>
        <div class="ml-auto" style="padding-right:2%">
            <ul class="navbar-nav">
            {% if app.user %}
                <li class="nav-item">
                    <span class="nav-link">{{ app.user.piece }} Pièce(s)</span>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('logout') }}">Déconnexion</a>
                </li>
            {% endif %}
            </ul>
        </div>
    </nav>
{% endblock %}
